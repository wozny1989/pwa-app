{{page-title "PWA APP"}}

<div class="app-wrapper">
  <LinkTo @route="index" {{on "click" (fn this.toggleMenu true)}}><h1 class="header">HEADER</h1></LinkTo>

  {{outlet}}

  <Menu
    @showMenu={{this.showMenu}}
    @onClickItem={{this.toggleMenu}}
  />
</div>
